## Full list of removed and changed API (by module, alphabetically) {{ '{#api-removed-and-changed}' }}

This sections contains the full list of APIs that have been changed or removed between Silverstripe CMS {{ fromVersion }} and {{ toVersion }}. You most likely don't need to read the entire list. But it can be a useful reference to have open when upgrading a project or module.

<details>
<summary>Reveal full list of API changes</summary>

<!-- markdownlint-disable proper-names enhanced-proper-names -->

{% for module, messages in apiChanges  %}
### `{{ module }}`

{% for message in messages %}
- {{ message }}
{% endfor %}

{% endfor %}

</details>
